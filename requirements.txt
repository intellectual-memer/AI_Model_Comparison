langchain==0.0.230  # Replace with the latest LangChain version
langchain-community==0.0.3  # Required for FAISS and other vectorstores
openai  # OpenAI library for GPT models and embeddings
sentence-transformers  # Required for Hugging Face embeddings
faiss-cpu  # FAISS for vector retrieval (CPU version)
bert-score  # For evaluation of answers
unstructured  # For parsing and loading PDF/docx
pypdf2  # For PDF parsing
numpy  # Required for numerical computations
pandas  # For DataFrame handling
requests  # For API requests
